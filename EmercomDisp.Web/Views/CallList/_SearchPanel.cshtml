@model EmercomDisp.Web.Models.Calls.CallSearchModel

@using (Ajax.BeginForm("Search", new AjaxOptions { UpdateTargetId = "result" }))
{
    @Html.AntiForgeryToken()
    
<div class="form-inline">
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <p>
        Id: @Html.EditorFor(model => model.Id, new { htmlAttributes = new { @class = "form-control" } })


        Address: @Html.EditorFor(model => model.Address, new { htmlAttributes = new { @class = "form-control" } })

        Category: @Html.DropDownListFor(model => model.Category, Model.Categories, "All")
        <input type="submit" value="Search" class="btn btn-default" />
    </p>
</div>
}